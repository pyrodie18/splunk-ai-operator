# This rule is not used by the project splunk-ai-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the ai.splunk.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: splunk-ai-operator
    app.kubernetes.io/managed-by: kustomize
  name: aiservice-editor-role
rules:
- apiGroups:
  - ai.splunk.com
  resources:
  - aiservices
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ai.splunk.com
  resources:
  - aiservices/status
  verbs:
  - get
