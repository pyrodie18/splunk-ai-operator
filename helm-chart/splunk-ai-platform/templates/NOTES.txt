1. Your AIPlatform custom resource has been created:
   kubectl get aiplatform {{ include "splunk-ai-platform.fullname" . }} -n {{ include "splunk-ai-platform.namespace" . }}

2. If you enabled Ingress:
   * Host: {{ (index .Values.ingress.hosts 0).host }}
   * Port: 80 / 443
   * Try: curl http://{{ (index .Values.ingress.hosts 0).host }}
